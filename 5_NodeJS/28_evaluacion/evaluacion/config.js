var config = {
  database: {
    host: "localhost", 
    user: "phpmyadmin", 
    password: "myadmin123",
    port: 3306, 
    db: "basefinalnode"
  },
  server: {
    host: "127.0.0.1",
    port: "3000"
  }
};

module.exports = config;
